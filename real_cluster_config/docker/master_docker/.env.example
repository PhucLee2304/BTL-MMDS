# Master node LAN configuration
# Set MASTER_HOST to the IP address of this master machine on the LAN
# Set WORKER_HOST_IP to the same IP (master runs DataNode in hybrid mode)

MASTER_HOST=192.168.1.111
WORKER_HOST_IP=192.168.1.111
