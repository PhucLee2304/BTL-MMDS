# Worker node LAN configuration
# Set MASTER_HOST to the IP address of the MASTER machine on the LAN
# Set WORKER_HOST_IP to THIS machine's LAN IP (used for DataNode advertisement)

# Example for worker on 192.168.1.112:
MASTER_HOST=192.168.1.111
WORKER_HOST_IP=192.168.1.112
